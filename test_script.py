import pandas as pd
import numpy as np
import pickle
import warnings
warnings.filteraxis = lambda *a, **k: None
warnings.simplefilter("ignore")

# Since pipeline is in memory of notebook, let's write a script to check it using notebook variables.
